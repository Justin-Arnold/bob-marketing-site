---
import { storyblokEditable } from '@storyblok/astro'
import StoryblokComponent from '@storyblok/astro/StoryblokComponent.astro'

const { blok } = Astro.props
---
<!-- <Hero>
    <span slot="pre-heading">
        {
            blok.preheading?.map((subBlok: any) => {
                return <StoryblokComponent blok={subBlok} />
            })
        }
    </span>
    <span slot="heading">
        {blok.heading}
    </span>
    <span slot="subtext">
        {blok.subheading}
    </span>
    <span class="flex items-start gap-2 w-full" slot="call-to-action">
        { () => {
            if (blok.secondaryAction) {
                return <a class="btn btn-secondary btn-outline max-w-fit">{blok.secondaryAction}</a>
            }
        }}
        <a class="btn btn-primary max-w-fit">{blok.callToAction}</a>
    </span>
</Hero> -->
<div class="hero" {...storyblokEditable(blok)}>
    <div class="content hero-content flex-col items-start md:flex-row md:items-center">
        <!-- Banner Header -->
        <h2 class="max-w-[500px]">
            {blok.heading}
        </h2>
        <!-- Banner Subtext & CTA -->
        <div class="flex flex-col gap-4 text-left">
            <p class="sub-2">
                {blok.subtext}
            </p>
            <a href={blok.call_to_action_link} class="btn btn-primary max-w-fit">
                {blok.call_to_action}
            </a>
        </div>
    </div>
</div>